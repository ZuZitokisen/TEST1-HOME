この版で反映した内容
- 39, 38, 40 をホーム画面 / ツール選択画面へ実画像で反映
- 35, 37 を各ツール左上ロゴへ実画像で反映
- 26CLOSE を設定モーダルへ実画像で反映
- 25CONECT, 29MENU も assets に同梱

既存機能との競合・留意点
- tools/treca.html と tools/time-marker.html は既存FIX仕様優先で保持している。
- そのため、内部には外部CDN依存がまだ残っている箇所がある。
- これは「FIX済み内容を主観で崩さない」ことを優先した結果で、完全ローカル化は次段階での接続作業になる。
